import React, { useState } from "react";
import "./Home.scss"; // Importa los estilos
import { useNavigate } from 'react-router-dom';

const Home: React.FC = () => {
    return (     
        <section>
            <div>
                Home
            </div>
        </section>
    );
}

export default Home;
